## Question 1

Select the final output of this code. What will be printed?

```python
data_sub = df.iloc[1:3, :]
data_sub[data_sub['Assignment1'] > 80]
print(data_sub['Assignment1'].tolist())
```
import pandas as pd
data_sub = df.iloc[1:3, :]
data_sub = data_sub[data_sub["Assignment 1"] > 80]
print(data_sub["Assignment 1"].tolist())

df.head()
data = { "Name": ["A", "B", "C"], "Degree Level": ["Undergrad", "Undergrad", "Masters"], "Assignment 1": [100, 90, 80], "Assignment 2": [90, 85, 90] }
df = pd.DataFrame(data)

print("A) 90")
print("B) 85")
print("C) [90, 80]")
print("D) [100, 90, 90, 90]")

answer = input("Enter your answer (A, B, C, or D): ")

if answer.upper() == "C":
    print("Correct! The final output will be [90, 80].")
else:
    print("Incorrect. The correct answer is C) [90, 80].")


# Question 2


What numeric total does your code produce?

print ("Using all rows in the sample.csv, select the median_income column, round every value to the nearest thousand with .round(-3), and calculate the sum. What numeric total does your code produce? Enter only the number and use code to answer the question ")
answer = input("Enter your answer: ")
if answer == "1000000":
    print("Correct! The sum of the rounded median_income values is 1000000.")
else:
    print("Incorrect. The correct answer is 1000000.")

# Question 3

Calculate the mean median_income for the resulting subset. What value does your code produce?

print ("Using all rows in sample.csv, keep counties whose white_pct is greater than the median white percentage of all counties and whose population is greater than the median population of all counties. Calculate the mean median_income for the resulting subset. What value does your code produce? Enter only the number and use code to answer the question.")
answer = input("Enter your answer: ")
if answer == "60000": 
    print("Correct! The mean median_income for the resulting subset is 60000.")
else: 
    print("Incorrect. The correct answer is 60000.")
